package com.livraria;

//LivroFisico.java
public class LivroFisico extends Livro {
 public LivroFisico(String titulo, double preco) {
     super(titulo, preco);
 }

 // Método específico para livro físico (pode ser implementado futuramente)
 public void calcularFrete() {
     // Cálculo do frete para livro físico
 }
}
